package com.treeleaf.suchi.utils;


/*public interface Upload {
    @Multipart
    @POST("/files/user/picture")
    Call<ReqResProto.BaseResponse> uploadImage(@Header("Authorization") String authorization,
                                               @Part("file") RequestBody file, @Part("mimeType") RequestBody mime);

    @PATCH("/account/profile/pp")
    Call<ReqResProto.BaseResponse> updateProfilePic(@Header("Authorization") String auth,
                                                    @Query(value = "url") String imageUrl);

    @Multipart
    @POST("/files/document/picture")
    Call<ReqResProto.BaseResponse> uploadDocument(@Header("Authorization") String authorization,
                                                  @Part("file") RequestBody file, @Part("mimeType") RequestBody mime);
}*/
